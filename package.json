{
  "author": "sendstreak.com",
  "description": "In-memory cache with a self revalidating approach",
  "devDependencies": {
    "mocha": "10.0.0",
    "typescript": "4.7.4"
  },
  "directories": {
    "test": "test"
  },
  "keywords": [
    "authentication",
    "in-memory",
    "cache"
  ],
  "license": "Apache-2.0",
  "main": "./lib",
  "name": "@sendstreak/revalidating-cache",
  "repository": {
    "type": "git",
    "url": "https://github.com/sendstreak/revalidating-cache.git"
  },
  "scripts": {
    "build": "rm -rf ./lib && tsc",
    "prepack": "npm run build",
    "test": "mocha"
  },
  "types": "./lib/index.d.ts",
  "version": "1.0.1"
}
